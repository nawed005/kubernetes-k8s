apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin-config
  labels:
    app: pgadmin
data:
  PGADMIN_DEFAULT_EMAIL: "admin@resensepoc.com"
  PGADMIN_DEFAULT_PASSWORD: "resense_poc@123!@#"
  PGADMIN_PORT: "80"